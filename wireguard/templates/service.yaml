apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: {{ .Release.Name }}
  type: LoadBalancer
  ports:
    - protocol: UDP
      port: 51820
      targetPort: 51820